"""Sends alert + keyframe (face-blurred by default) + transcript; accepts 'ok' acks; rejects every
chat ID except TELEGRAM_GUARDIAN_CHAT_ID (hard rule 7).

Sprint: S3. See docs/context/architecture.md.
"""
