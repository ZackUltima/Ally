# Full runtime for the live app. Resolve on Python 3.11 only (CV/ML wheels lag on 3.13).
# torch/torchvision come from the CUDA wheel index — see TORCH_INDEX in the Makefile; confirm the
# cuXXX tag on https://pytorch.org/get-started/locally/ before the first install.
-r requirements-core.in

# vision (GPU)
torch>=2.3
torchvision>=0.18
ultralytics>=8.2
opencv-python>=4.9
onnxruntime-gpu>=1.17
mediapipe>=0.10        # candidate pose backend; YOLO vs MediaPipe decided in Sprint 1

# ml
scikit-learn>=1.4
lightgbm>=4.3
pandas>=2.2

# speech (CPU)
faster-whisper>=1.0
piper-tts>=1.2
sounddevice>=0.4.6
webrtcvad-wheels>=2.0.14

# llm / vlm — only ally/agent/llm_client.py imports this
anthropic>=0.30

# backend
fastapi>=0.110
uvicorn[standard]>=0.29
sqlmodel>=0.0.16
jinja2>=3.1
python-telegram-bot>=21.0
python-multipart>=0.0.9
